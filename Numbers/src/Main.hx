class Main {
	static function main() {

		//Create ourFirstNumber variable
		var ourFirstNumber;

		//Add the value of 5 to ourFirstNumber
		ourFirstNumber = 5;

		//Change (overwrite) the value from 5 to -100
		ourFirstNumber = -100;

		//Change (overwrite) the value from -100 to 1000000
		ourFirstNumber = 1000000;

		//Output the data inside ourFirstNumber to the screen
		trace(ourFirstNumber);
	}
}
