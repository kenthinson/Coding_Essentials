class Main {
	static function main() {
		trace("Hello, world!");
		var ourString = "Hello Next Level Code!";
		trace(ourString);
		var ourSecondString = 'Hello!';
		trace(ourSecondString);
		var ourThridString = "Test string";
		var phrase = "Don't forget your quote";
		var phrase2 = "He said don't \"go to the supermarket\" then hung up the phone.";
		trace(phrase2.length);
	}
}
